# Copy this file to .env and fill in your own values.
# .env is gitignored — never commit real credentials, even local/dev-only ones.

# MariaDB credentials for the local EJBCA database (docker-compose.ejbca.yml)
EJBCA_DB_ROOT_PASSWORD=
EJBCA_DB_PASSWORD=
